// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/cybozu-go/scim/sample/ent/user"
	"github.com/cybozu-go/scim/sample/ent/x509certificate"
	"github.com/google/uuid"
)

// X509CertificateCreate is the builder for creating a X509Certificate entity.
type X509CertificateCreate struct {
	config
	mutation *X509CertificateMutation
	hooks    []Hook
}

// SetValue sets the "value" field.
func (xc *X509CertificateCreate) SetValue(s string) *X509CertificateCreate {
	xc.mutation.SetValue(s)
	return xc
}

// SetUserID sets the "user" edge to the User entity by ID.
func (xc *X509CertificateCreate) SetUserID(id uuid.UUID) *X509CertificateCreate {
	xc.mutation.SetUserID(id)
	return xc
}

// SetNillableUserID sets the "user" edge to the User entity by ID if the given value is not nil.
func (xc *X509CertificateCreate) SetNillableUserID(id *uuid.UUID) *X509CertificateCreate {
	if id != nil {
		xc = xc.SetUserID(*id)
	}
	return xc
}

// SetUser sets the "user" edge to the User entity.
func (xc *X509CertificateCreate) SetUser(u *User) *X509CertificateCreate {
	return xc.SetUserID(u.ID)
}

// Mutation returns the X509CertificateMutation object of the builder.
func (xc *X509CertificateCreate) Mutation() *X509CertificateMutation {
	return xc.mutation
}

// Save creates the X509Certificate in the database.
func (xc *X509CertificateCreate) Save(ctx context.Context) (*X509Certificate, error) {
	var (
		err  error
		node *X509Certificate
	)
	if len(xc.hooks) == 0 {
		if err = xc.check(); err != nil {
			return nil, err
		}
		node, err = xc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*X509CertificateMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = xc.check(); err != nil {
				return nil, err
			}
			xc.mutation = mutation
			if node, err = xc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(xc.hooks) - 1; i >= 0; i-- {
			if xc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = xc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, xc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (xc *X509CertificateCreate) SaveX(ctx context.Context) *X509Certificate {
	v, err := xc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (xc *X509CertificateCreate) Exec(ctx context.Context) error {
	_, err := xc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (xc *X509CertificateCreate) ExecX(ctx context.Context) {
	if err := xc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (xc *X509CertificateCreate) check() error {
	if _, ok := xc.mutation.Value(); !ok {
		return &ValidationError{Name: "value", err: errors.New(`ent: missing required field "X509Certificate.value"`)}
	}
	return nil
}

func (xc *X509CertificateCreate) sqlSave(ctx context.Context) (*X509Certificate, error) {
	_node, _spec := xc.createSpec()
	if err := sqlgraph.CreateNode(ctx, xc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (xc *X509CertificateCreate) createSpec() (*X509Certificate, *sqlgraph.CreateSpec) {
	var (
		_node = &X509Certificate{config: xc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: x509certificate.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: x509certificate.FieldID,
			},
		}
	)
	if value, ok := xc.mutation.Value(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: x509certificate.FieldValue,
		})
		_node.Value = value
	}
	if nodes := xc.mutation.UserIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: false,
			Table:   x509certificate.UserTable,
			Columns: []string{x509certificate.UserColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeUUID,
					Column: user.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.x509certificate_user = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// X509CertificateCreateBulk is the builder for creating many X509Certificate entities in bulk.
type X509CertificateCreateBulk struct {
	config
	builders []*X509CertificateCreate
}

// Save creates the X509Certificate entities in the database.
func (xcb *X509CertificateCreateBulk) Save(ctx context.Context) ([]*X509Certificate, error) {
	specs := make([]*sqlgraph.CreateSpec, len(xcb.builders))
	nodes := make([]*X509Certificate, len(xcb.builders))
	mutators := make([]Mutator, len(xcb.builders))
	for i := range xcb.builders {
		func(i int, root context.Context) {
			builder := xcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*X509CertificateMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, xcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, xcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, xcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (xcb *X509CertificateCreateBulk) SaveX(ctx context.Context) []*X509Certificate {
	v, err := xcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (xcb *X509CertificateCreateBulk) Exec(ctx context.Context) error {
	_, err := xcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (xcb *X509CertificateCreateBulk) ExecX(ctx context.Context) {
	if err := xcb.Exec(ctx); err != nil {
		panic(err)
	}
}
